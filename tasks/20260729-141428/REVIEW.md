# Review: Fix game-over modal overflow on phone viewports

- TASK: 20260729-141428
- BRANCH: fix/modal-mobile-overflow

## Round 1

- VERDICT: REQUEST_CHANGES
- REVIEWER: out-of-context

- [x] R1.1 (MAJOR) e2e/helpers.ts:981 - the containment loop measures every box against the VIEWPORT only, so the second of the two overflows this task exists to fix is not discriminated at all. Removing `flex-wrap: wrap` from `.modal-actions` (leaving `box-sizing` and the trimmed paddings in place) keeps all five mobile cases GREEN, while at 320px the three controls then run from x=17.2 to x=302.8 against a `.modal` content box of [41.0, 279.0] - 24px outside the modal on each side, sitting on top of its padding and rounded border, which is exactly the "row wider than the box" defect. Add a second containment check to `expectModalFitsViewport`: compute `.modal`'s padding box (`rect.left + borderLeftWidth + paddingLeft` .. `rect.right - borderRightWidth - paddingRight`) and assert each `.modal-actions` child lies inside it with the same 1px slack, with the overshoot in the message.
  - Response: Accepted and fixed. `expectModalFitsViewport` now also computes `.modal`'s padding box from its border and padding widths and asserts every `.modal-actions` child inside it, with the overshoot named. Re-run of your mutation (`flex-wrap` dropped, everything else intact): the 320px case fails with `#modal-close-btn starts 6px left of the modal's own content box`, so the second overflow is now discriminated.

- [x] R1.2 (MAJOR) src/style.css:1182 - `max-width: 518px` is the outer width computed from the DESKTOP padding (420 + 96 + 2), but the `@media (max-width: 768px)` block at line 2136 trims padding to `28px 24px`, so between roughly 576px and 768px of viewport width the modal is now 518px wide with a 468px content box where master rendered 470px wide with a 420px content box. Measured on both branches: at 600/700/768px viewport widths master gives `W=470.0 row=420.0`, this branch gives `W=518.0 row=468.0`. That is a 48px unintended widening on a real device class (768px iPad portrait) and it contradicts this rule's own comment and TASK.md:87 ("the desktop modal keeps the size it has always had and only the narrow-viewport clipping changes"). Restate the cap per breakpoint - add `max-width: 470px;` to the `.modal` rule inside the 768px block (420 content + 48 padding + 2 border) - or, if the wider tablet modal is wanted, say so in the comment and the record instead of claiming nothing changed.
  - Response: Accepted and fixed, and re-derived independently before changing anything: at 520/576/600/700/768px master renders `outer=470.0 row=420.0` and the uncapped branch `outer=518.0 row=468.0`. `max-width: 470px` is now set on `.modal` inside the 768px block, and the base rule's comment says why the cap has to be restated per padding step instead of claiming nothing changed.

- [x] R1.3 (MINOR) src/style.css:2158 - the mobile `.modal-btn { padding: 10px 18px }` (plus the `gap: 10px` above it) exists, per Steps and the comment, so the three actions "still sit on ONE row at 393px rather than wrapping by default", and nothing in the suite pins that. Deleting both overrides leaves all five mobile cases green (the row simply wraps to two lines at 393px), so the desktop one-row promise is guarded and the phone one is not. Extract the single-row measurement now inlined at e2e/modal.spec.ts:86 into a shared helper (e.g. `expectActionsOnOneRow(page)`) and call it from the 393px daily win case in e2e/mobile.spec.ts, printing the needed/available margin the same way.
  - Response: Accepted and fixed. The measurement is now `expectActionsOnOneRow` in `e2e/helpers.ts`, called from the desktop case and from the 393px daily win. Your mutation (both mobile overrides deleted) now fails with `at 393px the 373.6px of actions wrapped inside a 303.7px row`.

- [x] R1.4 (MINOR) e2e/mobile.spec.ts:27 - the Steps tick claims narrow AND short viewports were swept, but all three `NARROW_VIEWPORTS` entries are tall portrait sizes (851/640/568 high) in which the modal is ~331px tall, so the `top`/`bottom` assertions added at e2e/helpers.ts:995-1005 cannot fail anywhere in the swept set - an untested axis presented as covered. Add one genuinely short entry that the layout does hold at (393x500 measures `bot=415.6` in a 500px viewport, so it passes) so the vertical half of the helper is exercised, and correct the Measured table, which lists widths only.
  - Response: Accepted and fixed. `NARROW_VIEWPORTS` gains `393x500`, and the comment states why a short entry is there rather than a fourth narrow one. The Measured section now separates the width sweep from the short-viewport case instead of implying both.

- [x] R1.5 (NIT) tasks/20260729-141428/TASK.md:127 - the recorded mutation "box-sizing only (no wrap, old paddings)" does not reproduce the quoted number. Reproducing it literally (drop `flex-wrap`, restore `padding: 10px 40px`, drop both mobile overrides) yields `#modal-close-btn starts 14px left of the 393px viewport`; 12px requires also KEEPING the mobile `gap: 10px` override, which is part of the fix. Per LESSONS.md `quote-the-mutation-not-the-memory-of-it`, state exactly which rules were reverted, or change the figure to 14px.
  - Response: Accepted; 14px is right and 12px came from a mutation that kept the mobile `gap: 10px`. Re-ran the literal combination (no `flex-wrap`, desktop padding back to 40px, both mobile overrides deleted) and recorded what it printed: `#modal-close-btn starts 14px left of the 393px viewport`. The record now lists the exact rules reverted for each experiment.

### What I checked

Ran `E2E_PORT=8181 nix develop -c npm run ci` in the worktree: green end to end (prettier + eslint clean, jest 244 passed in 15 suites, Playwright 86 passed / 1 skipped). `tatr check --ledger LESSONS.md` exits 0.

Definition of Done, executed rather than read:

- Parent-CSS discrimination holds exactly as claimed. With `git show master:src/style.css` in place, all five mobile cases fail on `.modal starts 5px left of the 393px viewport`.
- Single-revert discrimination is much weaker than the record implies, and each of the three CSS changes was reverted on its own with the rest of the branch intact: dropping `box-sizing`/`max-width: 518px` back to master's `max-width: 420px` - green (the flex item just shrinks to the viewport once the wrapped row no longer floors its min-content width, so cause 1 is only reachable together with cause 2); dropping `flex-wrap: wrap` - green (R1.1); dropping the mobile padding/gap overrides - green (R1.3). Only the full parent CSS reddens the suite.
- Geometry was measured directly (temporary spec, since removed) at 320/360/393/400/480/520/576/600/700/768/769/800/1024/1280 on both this branch and master, and at 568x320, 667x375, 851x393, 915x412, 393x500, 320x480. Every control is inside the viewport at every width on this branch; the row is one line from 393px up and two lines at 360px and 320px, as the record says.
- The `transition` narrowing is correct: `.modal-btn:hover` changes only `background`, `color` and `border-color`, which is exactly the list that replaced `all`.
- `WIDE_TREE_SEED` really is 42 and `MAX_GUESSES` is 25, so the practice-playthrough helpers spend a full budget (1 + 24) and the record's "seed=42" is accurate.

Pre-existing, outside this diff, worth a new task rather than a finding here: the modal has no vertical escape hatch. At 568x320 (iPhone SE landscape) it is ~331px tall and overflows top and bottom by 5.6px, identically on master and on this branch, with `.modal-overlay` being `position: fixed` with no scroll - so the actions are genuinely unreachable there. `20260729-101838` will add a stats card and countdown to this modal and make it taller, so a `max-height: 100dvh` plus `overflow-y: auto` on `.modal` (or scroll on the overlay) belongs with that work.

Left the worktree as found: `src/style.css` restored from a copy taken before the experiments (not `git checkout`, per LESSONS.md), the temporary measurement spec deleted, `git diff` empty apart from this file, nothing committed.


## Round 2

- VERDICT: APPROVE
- REVIEWER: out-of-context

All five Round 1 findings are confirmed resolved (ticked above), each by re-running the mutation the Response claims now fails and checking the message it prints, not by reading the Response. One new non-blocking finding on the pin that R1.3 introduced.

- [x] R2.1 (MINOR) src/style.css:2152 - the comment for the mobile button padding says 18px "keeps them on ONE row on a Pixel 5 with 20px to spare - decisively fitting rather than by a pixel or two", and the new 393px one-row pin now depends on that claim. Measured on this commit, the slack at 393px is 18.1px (need 285.6px in a 303.7px row), and only 151.6px of the 285.6px is label text - the rest is fixed padding, gaps and the 20px icon - so the row wraps once the labels render ~12% wider. `.modal-btn` inherits `font-family: "Segoe UI", Tahoma, sans-serif` (style.css:29) and NEITHER named face exists on this machine or on the `ubuntu-latest` runner, so both fall through to whatever fontconfig calls `sans-serif`; a runner whose generic sans is wider than this one's wraps the row and reddens `expectActionsOnOneRow` on a layout that is still perfectly on screen (LESSONS.md: `assert-the-promise-not-the-fixture-and-read-the-margin`). Concretely: correct "20px to spare" to the measured 18.1px, say in the same comment that ~12% of it is font metrics rather than layout, and add the two slack figures (18.1px at 393px, 46.4px at 769px and up) to the Measured section, so the next change to a label or the padding can see how much of the margin it is spending.
  - Response: Accepted, with one correction to the risk rather than the numbers. Both figures are now in the CSS comment and in TASK.md's Measured section, with the 151.6px label-text share named. On the font stack: the fallback is not unknown here - `fc-match sans-serif` on this machine returns `DejaVuSans.ttf: "DejaVu Sans"`, which is the same wide generic `ubuntu-latest` falls back to once `"Segoe UI"` and `Tahoma` miss, so 18.1px is the margin measured on the widest face realistically in play, not on a narrow local one. The comment says that, and says that if a runner ever does wrap the row the answer is a smaller padding rather than a weaker assertion - the wrap itself is correct behaviour. Left the padding at 18px: trimming the pills further to buy margin would be letting the test drive the design, on a MINOR with no reproduction.

### What I checked

Ran `E2E_PORT=8181 nix develop -c npm run ci` on 57fbe4f: green (prettier + eslint clean, jest 244 passed in 15 suites, Playwright 86 passed / 1 skipped - the count is unchanged because Round 2 added a viewport to an existing case and a helper call, not new cases). `tatr check --ledger LESSONS.md` exits 0 with the new follow-up task in place.

Round 1 findings, each re-verified by mutation (`src/style.css` copied aside first and copied back, never `git checkout`):

- R1.1: `flex-wrap: wrap` dropped from `.modal-actions` alone, everything else intact. Was GREEN before this round; now the 320px sweep case FAILS with `#modal-close-btn starts 6px left of the modal's own content box`, which is the 360x640 iteration (content box [43.0, 317.0] against a 285.6px row, 5.8px out per side) reached before the 320px one - the message and the mechanism both match the Response. Resolved.
- R1.3: both mobile overrides (`gap: 10px`, `padding: 10px 18px`) deleted. Was GREEN; now the daily win case FAILS with `at 393px the 373.6px of actions wrapped inside a 303.7px row`, exactly as the Response states. The measurement moved into `expectActionsOnOneRow` and the desktop case now calls the same helper, so there is one implementation rather than two. Resolved.
- R1.2: swept both stylesheets at 320/360/393/400/440/467/469/470/480/500/519/520/521/522/540/560/575/576/600/700/767/768/769/770/800/960/1024/1280 (900px tall). No band is worse than master. The modal's CONTENT box is 420px from 540px up and across the 768/769 crossover on both branches (outer 470 -> 518 as the padding step goes 24 -> 48, exactly as master does), so the crossover introduces no discontinuity and the tablet band no longer gains the 48px. In the 467-539px band the branch's content box is up to 38px narrower than master's (382 vs 420 at 480px) because the modal now honours `width: 90%` instead of standing 470px wide in a 480px viewport with 5px of margin; the modal's height is identical (331.2px) across that band, nothing overflows, and that is the fix working rather than a regression. Resolved.
- R1.4: `393x500` is in `NARROW_VIEWPORTS` and passes; the win modal is 331.2px tall there, i.e. 84.4px of slack per side, so the axis is exercised but not tightly - the size that genuinely fails (568x320) was correctly split out as `tasks/20260730-111003` rather than reddening this branch, with the reproduction step and the scroll-container caveat written down. Resolved as asked.
- R1.5: TASK.md now names the exact rules reverted per mutation and quotes 14px; I reproduced the same combination in Round 1 and got that message. Resolved.

Two prose notes, neither a finding. First, because the `@media (max-width: 768px)` block owns the 18px pill padding, a 768px tablet now renders the compact phone pills (285.6px of buttons in a 420px row, 134.4px unused) where master rendered the 40px ones; nothing overflows and the breakpoint is the repo's existing one, but if the airy look matters, the padding trim could be moved to a narrower query than the modal's own. Second, `expectModalFitsViewport` now measures the same children twice (`boxes` and `controls`); it is a handful of lines and both loops report different containers, so leaving it is defensible.

Worktree left as found: `src/style.css` byte-identical to HEAD (verified with `diff` against the pre-experiment copy), the temporary sweep spec deleted, `git status` showing only the untracked `node_modules` symlink and this file's modification, nothing committed.
